{
  "name": "chef-asg-cleanup",
  "version": "0.1.0",
  "description": "Set up AWS Lambda to clean up Chef Nodes and Clients after Autoscaling events",
  "main": "index.js",
  "engine": "node 0.10.36",
  "dependencies": {
    "chef-api": "^0.5.2"
  },
  "devDependencies": {
    "grunt-aws-lambda": "^0.11.0",
    "grunt": "^0.4.5"
  },
  "bundledDependencies": [
    "chef-api"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Reedy",
  "license": "Apache-2.0"
}
